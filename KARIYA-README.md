# کاریا دسک — نرم‌افزار اختصاصی ویندوز

نسخه‌ی برندشده‌ی RustDesk با سه چیزِ اضافه که مالِ خودمان است:

1. **ورود اجباری** — تا کاربر با شماره موبایل و رمز وارد نشود، شناسه و رمز اتصال را نمی‌بیند.
2. **بنر تبلیغ** — زیر کادر رمز، با تصویر و لینک کلیک‌شدنی. محتوا از سرور می‌آید، پس عوض
   کردن تبلیغ **نسخه‌ی جدید لازم ندارد**.
3. **سرور هاردکد** — `desk.kariyahesab.com` و کلید عمومی ما از قبل داخل برنامه است؛ مشتری
   هیچ تنظیمی نمی‌کند.

## چرا فورک، نه نوشتن از صفر

هسته‌ی RustDesk (کدک تصویر، NAT traversal، رله، درایور ورودی) سال‌ها کار روی آن رفته.
نوشتن از صفر یعنی چند نفر-ماه برای رسیدن به همان جایی که امروز هستیم. پس سورس بالادست را
دست‌نخورده می‌گیریم و فقط تغییرات خودمان را رویش می‌زنیم.

⚠️ **لایسنس:** RustDesk با AGPL-3.0 است. نسخه‌ی تغییریافته را که پخش کنیم باید سورسش در
دسترس باشد. نام و لوگوی RustDesk هم علامت تجاری است و باید کامل برداشته شود.

## ساختار

```
kariya/                    کد دارتِ خودمان (داخل flutter/lib/kariya/ کپی می‌شود)
  kariya_api.dart          ارتباط با سرور: ورود، توکن، بنر
  login_gate.dart          صفحه‌ی ورود
  ad_banner.dart           بنر تبلیغ
patches/apply.py           تغییرات را روی یک سورس تمیز RustDesk اعمال می‌کند
upstream/                  کلون سورس RustDesk (در گیت نگه داشته نمی‌شود)
```

سمت سرور، همه‌چیز داخل پنل کاریا دسک است (`C:\ai\kariya-desk`، روی سرور `/opt/kariyadesk`):

| مسیر | کار |
|---|---|
| `POST /api/app/login` | ورود با موبایل و رمز → توکن ۹۰ روزه |
| `POST /api/app/ping` | بررسی معتبر بودن توکن |
| `GET /api/app/ad` | بنر فعال (تصویر + لینک) |
| `POST /api/app/click` | شمردن کلیک روی تبلیغ |
| `/api/admin/*` | مدیریت کاربران و تبلیغ‌ها از داخل پنل |
| `/ads/<file>` | تصویر بنرها |

دیتابیس این بخش: `/var/lib/kariyadesk/app/app.sqlite3` (مالکش کاربر پنل است، جدا از
دیتابیس متریک که کالکتورِ روت می‌نویسد).

## ساختن نسخه‌ی ویندوز

ساخت RustDesk روی ویندوزِ محلی عملاً شدنی نیست (نیاز به MSVC + vcpkg + Flutter + LLVM و
چند ساعت). راهِ درست، GitHub Actions است — همان کاری که برای اپ تلفن هم کردیم.

```bash
# ۱) سورس تمیز روی تگ مشخص
git clone --depth 1 -b 1.4.9 --recurse-submodules https://github.com/rustdesk/rustdesk upstream

# ۲) تغییرات ما
python3 patches/apply.py upstream

# ۳) پوش روی مخزن خودمان و اجرای ورک‌فلوی ویندوز
```

**مرحله‌ای که به حساب گیت‌هاب نیاز دارد:** توکن `gh` روی این ویندوز منقضی شده
(`gh auth status` می‌گوید invalid) و کلیدهای SSH موجود همه Deploy Key هستند و فقط به یک
مخزن مشخص دسترسی دارند. برای ساخت، یکی از این دو لازم است:

- `gh auth login` روی همین ویندوز (یک‌بار، دستی)، یا
- ساخت مخزن `kariya-desk-client` در گیت‌هاب و افزودن یک Deploy Key با اجازه‌ی نوشتن.

## وضعیت

- ✅ سمت سرور کامل و روی سرور زنده تست شد (ساخت کاربر، ورود، توکن، بنر، شمارش کلیک)
- ✅ اسکریپت تغییرات روی سورس واقعی RustDesk 1.4.9 تست شد و همه‌ی نقاط را درست می‌زند
- ✅ کد دارتِ ورود و بنر نوشته شد
- 🔺 مانده: اجرای ساخت روی GitHub Actions و گرفتن اولین EXE
- 🔺 مانده: لوگو و آیکون کاریا (فایل تصویری لازم است)
- 🔺 مانده: پنل مدیریت تبلیغ و کاربران در رابط کاربری پنل (API‌اش آماده است)
